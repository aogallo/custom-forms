import { style } from '@vanilla-extract/css'

export const contentWizard = style({
  height: '80vh',
})

export const contentButtons = style({
  display: 'flex',
  justifyContent: 'space-between',
})
